<?xml version="1.0" encoding="utf-8"?>
<!--
    android/app/src/main/AndroidManifest.xml

    Este manifesto configura o app como um LAUNCHER nativo de multimídia
    automotiva: ele intercepta o botão HOME da central, além de solicitar
    todas as permissões necessárias para GPS, Bluetooth, Wi-Fi e para
    conseguir abrir apps de terceiros instalados (Waze, Google Maps, Spotify).
-->
<!--
    O namespace do módulo (equivalente ao antigo atributo "package" abaixo)
    agora é definido só em android/app/build.gradle (gerado automaticamente
    pelo `flutter create --org com.neoncar ...` no workflow). O Android
    Gradle Plugin 8+ não aceita mais declarar o pacote aqui — se declarado,
    o build falha com "Setting the namespace via the package attribute in
    the source AndroidManifest.xml is no longer supported".
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== PERMISSÕES ==================== -->

    <!-- Localização real (antena GPS física do carro) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Rede / Wi-Fi (gerenciador de conectividade) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Leitura da pasta externa de mapas (SD/pendrive) escolhida em
         Configurações. MANAGE_EXTERNAL_STORAGE é a permissão especial de
         "acesso a todos os arquivos" exigida a partir do Android 11 para
         ler arquivos fora da pasta própria do app. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Chamadas viva-voz sem depender de CarPlay/Android Auto: detectar
         chamada tocando e atender/encerrar via TelecomManager nativo
         (ANSWER_PHONE_CALLS não exige que o app seja o discador padrão). -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />

    <!-- Câmera de ré (quando a central expõe a entrada de vídeo como uma
         câmera Android comum, via CameraX/Camera2). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Bluetooth (gerenciador de conectividade) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Necessário para conseguir enxergar/abrir outros apps instalados (Waze, Maps, Spotify) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Mantém o launcher "sempre vivo" em foreground quando necessário -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:name="${applicationName}"
        android:label="Neon Car Launcher"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false"
        android:largeHeap="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:excludeFromRecents="false"
            android:stateNotNeeded="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />

            <!-- Intent padrão de inicialização do app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- INTERCEPTACAO DO BOTAO HOME:
                 Esta e a categoria que faz o app se registrar como
                 tela inicial da multimidia, concorrendo com (ou
                 substituindo) o launcher padrao do sistema. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>
</manifest>
